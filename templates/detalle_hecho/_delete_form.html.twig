<form method="post" action="{{ path('detalle_hecho_delete', {'id': detalle_hecho.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_hecho.id) }}">
    <button class="btn">Delete</button>
</form>
